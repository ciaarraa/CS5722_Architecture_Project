package com.Interceptor;
// Same as Interceptor interface from Ciara's example really except that JJ said to have 'log' in here
public interface Interceptor {
    void log(PaymentEventContextObject contextObject);
}
